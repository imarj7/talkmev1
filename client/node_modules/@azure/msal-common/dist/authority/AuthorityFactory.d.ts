import { Authority } from "@azure/msal-common/src/authority/Authority";
import { INetworkModule } from "@azure/msal-common/src/network/INetworkModule";
import { ICacheManager } from "@azure/msal-common/src/cache/interface/ICacheManager";
import { AuthorityOptions } from "@azure/msal-common/src/authority/AuthorityOptions";
import { Logger } from "@azure/msal-common/src/logger/Logger";
import { IPerformanceClient } from "@azure/msal-common/src/telemetry/performance/IPerformanceClient";
/**
 * Create an authority object of the correct type based on the url
 * Performs basic authority validation - checks to see if the authority is of a valid type (i.e. aad, b2c, adfs)
 *
 * Also performs endpoint discovery.
 *
 * @param authorityUri
 * @param networkClient
 * @param protocolMode
 * @internal
 */
export declare function createDiscoveredInstance(authorityUri: string, networkClient: INetworkModule, cacheManager: ICacheManager, authorityOptions: AuthorityOptions, logger: Logger, correlationId: string, performanceClient?: IPerformanceClient): Promise<Authority>;
//# sourceMappingURL=AuthorityFactory.d.ts.map